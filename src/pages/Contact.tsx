import { observer } from 'mobx-react';
import React from 'react';

const Contact = () => {
  return <div style={{ background: 'white', width: '80vw', height: '80vh' }}></div>;
};
export default observer(Contact);
